# Trusted deployment configuration, not caller-controlled request fields.
IO_COMPANY_CD=
IO_ENVIRONMENT=DEVELOPMENT
# Explicit read-only PostgreSQL credentials; no default host or credentials.
IO_POSTGRES_DSN=
